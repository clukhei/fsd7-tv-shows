<div class="d-flex mt-5 align-items-center">
    <img src="{{record.image}}" alt="img" class="img-fluid mr-5">
    <div class="d-flex flex-column justify-content-between">
        <div>
            <h1>{{record.name}}</h1>
            {{{record.summary}}}
            <code>{{record.ratings}}</code>
        </div>
        <div class="d-flex justify-content-between">
            
            <form action="/" method="GET">
                <button class="btn-warning btn" type="submit">Back</button>
            </form>
            {{#if hasLink}} 
            <form>
            <button class="btn-primary btn" formaction="{{record.official_site}}">Link to Site</Button>
            </form>
            {{/if}}
        </div>
    </div>
</div>